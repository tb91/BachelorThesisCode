package projects.reactiveSpanner.nodes.messages;

import java.util.UUID;

import projects.reactiveSpanner.nodes.messageHandlers.SubgraphStrategy.EStrategy;
import projects.reactiveSpanner.nodes.nodeImplementations.PhysicalGraphNode;

/**
 * Simple approach to divide an abstract message into
 * beacon and beaconless message. 
 * 
 * @author Tim, cyron
 *
 */
public abstract class BeaconMessage extends AbstractMessage{

	
	protected BeaconMessage(final UUID tcID, final PhysicalGraphNode 
			transmitter, final EStrategy strategy){
		super(tcID, transmitter, strategy);
		
	}
	


}
